# strict conflicts.policy, throw erros instead of warnings when masking from
# other packages
options(conflicts.policy="strict")
# default:
# options(conflicts.policy = NULL)
# warning only:
# options(conflicts.policy = list(error=FALSE, warn=TRUE))
