# more verbose output, handy for installing and loading packages
options(verbose=TRUE)

# strict conflicts.policy, throw erros instead of warnings when masking from
# other packages
options(conflicts.policy="strict")
